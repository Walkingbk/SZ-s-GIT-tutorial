    # SZ's GIT Tutorial

A Git tutorial repository.

This git repository is created for SZ to practice basic operation of Git and GitHub. These skills are important for code-development, research and personal development. The readme.md serve both as a explanation of the repository, and as a storage of terminologies and takeaways that could be visited later. 


Terminologies:

--- Organized Terminologies ---

    --- Version Control Basics ---

    commit: The action of submitting one version of a code onto github. 
        Whenever you commit a file/code, git creates a snapshot of the repository in a "main" branch. 
        This forms a historic, retraceable chain of past versions

    Commit id: A string of number and text, generated by the Hash algorithm of Git, to bookeep/reference different past version of the file
        Commit id (or commit hash) can be long or short, both of them work the same 
        Commit id can be used in coding agent like Claude to reference, alter, or restore past versions 

    --- Undoing Changes ---

    git.reset: Force the repository back to a historic state using the commit id
        If the commit is already pushed to remote repo, this could lead to code lost
        Good for local branches that are not commited to remote repo    

    git.revert: A command that allow for the retraction of 1 historic version of the code without changing the rest of the branch
        Generate a commit to replace a previous commit
        Good for online-co-operated branches

    Discard: The action of discarding the uncommited changes to documents
        Good for: Undoing changes before a commit was made 

    --- Navigation & Inspection ---

    checkout (Detached): A functionality within the source control panel of VScode that allows you to view a past version of the code. 
        Not suited for creating new branch, but simply serve inspection purposes

    --- Repository Setup & Config ---

    .gitignore: A plain text file that tells Git which files or directories to ignore in a project
        Privacy: Help hide important local information when project is uploaded into github

    --- Branching & Collaboration ---

    Branch: The series of commits that were made in parallel with a designated "main" series. The act of "Branching" is essentially creating a copy of a commit node, then making changes to that copied branch. The new changes to the "new Branch" will not impact the codes of the main branch". This streamline collaboration, and prevents accidental changes to the source code that may corrupt its functioning

        Diagram showcasing the branching logic:

        ---[Commit 1]---[Commit 2]---o---[Commit 3]---[Commit 4]-----o----[Merged Commit 5]---
                                     |___[Commit A1]___[Commit A2]___|

    Merge: The action of combining multiple branches, perhaps created for collaboration purposes, into one main branch

    HEAD: A concept that refer to the specific commit that the current repository is located on. For example, in a theoretical tree with a branching point at commit 5, and 2 commits under the subranch, the user could select the current Head could be on "commit #5" of the main branch.


--- New Terminologies ---

    (Add new terms here)





Takeaways:

Commit can be a powerful tool to track changes in code during vibe coding. Whenever coding agents, like Claude Code, created a new functionality, we could commit a newer version.
This streamline bookeeping of historic versions, and restore retraceability of the codes
